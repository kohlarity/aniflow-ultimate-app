import React from 'react'
import ReactDOM from 'react-dom/client'
import AniFlow from './AniFlow.jsx' // Make sure the filename matches exactly!

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AniFlow />
  </React.StrictMode>,
)